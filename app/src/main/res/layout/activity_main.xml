<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/download_module"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Download Module"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/open_module"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Open Module"
        android:enabled="false"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toBottomOf="@+id/download_module"
        app:layout_constraintStart_toStartOf="@id/download_module"
        app:layout_constraintEnd_toEndOf="@id/download_module"
        />

</androidx.constraintlayout.widget.ConstraintLayout>